name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀

on:
  push:
    branches: [ main ]
  schedule:
    - cron: "0 6,18 * * *"   #09:00 и 21:00 по Минску (06:00 и 18:00 UTC)
  workflow_dispatch: {}

jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      #Для cron-запусков чекаутим строго main
      - name: Checkout main for scheduled runs
        if: ${{ github.event_name == 'schedule' }}
        uses: actions/checkout@v4
        with:
          ref: refs/heads/main

      #Для push/ручного запуска чекаутим ветку из события
      - name: Checkout for non-scheduled runs
        if: ${{ github.event_name != 'schedule' }}
        uses: actions/checkout@v4

      - name: Context info
        run: |
          echo "🎉 Triggered by: ${{ github.event_name }}"
          echo "🐧 Runner OS: ${{ runner.os }}"
          echo "🔎 Branch: ${{ github.ref }} | Repo: ${{ github.repository }}"

      - name: Print public IP
        run: |
          echo "Server Public IP:"
          curl -s https://api.ipify.org
          echo

      - name: List files
        run: |
          echo "Repository file list:"
          ls -alR "${{ github.workspace }}"

      - run: echo "🍏 Job status is ${{ job.status }}."
